{
  "name": "escape-regexp",
  "description": "Escape regular expression special characters",
  "version": "0.0.1",
  "keywords": ["escape", "regexp", "utility"],
  "dependencies": {},
  "component": {
    "scripts": {
      "escape-regexp": "index.js"
    }
  }
}